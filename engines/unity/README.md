# Unity Adapter

Unity is not the active MVP engine. This folder is reserved so the repo can add a Unity target later without changing the project architecture.

If activated, Unity should import the same scene manifests from `content/scene-data` and processed assets from `content/processed`.
